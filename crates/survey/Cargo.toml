[package]
name = "survey"
description = "What each zone's maps paint and place, for the catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
atlas = { path = "../atlas" }
blp = { path = "../blp" }
dbc = { path = "../dbc" }
image = { version = "0.25", default-features = false, features = ["png"] }
light = { path = "../light" }
m2 = { path = "../m2" }
model = { path = "../model" }
mpq = { path = "../mpq" }
rayon = "1.12"
terrain = { path = "../terrain" }
wdt = { path = "../wdt" }

[lints]
workspace = true
