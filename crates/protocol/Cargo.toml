[package]
name = "protocol"
description = "The native wire between cairn's client and server: frames, the messages each side sends, and their encoding"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true
