[package]
name = "atlas"
description = "Draws a zone of World of Warcraft 1.12.1 from above: the ground in its textures' colours, hill shading and water, and doodads and buildings as marks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
blp = { path = "../blp" }
dbc = { path = "../dbc" }
image = { version = "0.25", default-features = false, features = ["png"] }
mpq = { path = "../mpq" }
rayon = "1.12"
terrain = { path = "../terrain" }

[lints]
workspace = true
