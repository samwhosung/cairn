[package]
name = "cairn"
description = "The client: walks a window through your WoW 1.12.1 install, renders one shot of it to a PNG, or draws a zone of it from above"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
assert_no_alloc = "1.1"
atlas = { path = "../atlas" }
avian3d.workspace = true
bevy.workspace = true
catalog = { path = "../catalog" }
protocol = { path = "../protocol" }
server = { path = "../server" }
sound = { path = "../sound" }
world = { path = "../world" }

[dev-dependencies]
game = { path = "../game" }
mpq = { path = "../mpq" }

[lints]
workspace = true
