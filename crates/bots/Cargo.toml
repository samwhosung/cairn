[package]
name = "bots"
description = "Bots that load the server, and scripted scenarios with a verdict"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
catalog = { path = "../catalog" }
dbc = { path = "../dbc" }
game = { path = "../game" }
libm = "0.2"
mpq = { path = "../mpq" }
protocol = { path = "../protocol" }
rayon = "1.12"
server = { path = "../server" }
terrain = { path = "../terrain" }
wdt = { path = "../wdt" }
tokio = { version = "1.53", features = [
  "rt-multi-thread",
  "net",
  "io-util",
  "sync",
  "time",
] }

[lints]
workspace = true
