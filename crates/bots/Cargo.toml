[package]
name = "bots"
description = "Bots for the server: a crowd over TCP that checks what it is shown, and scenarios run in process on the server's clock, each ending in one verdict"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
dbc = { path = "../dbc" }
game = { path = "../game" }
libm = "0.2"
mpq = { path = "../mpq" }
protocol = { path = "../protocol" }
rayon = "1.12"
server = { path = "../server" }
terrain = { path = "../terrain" }
wdt = { path = "../wdt" }
tokio = { version = "1.53", features = [
  "rt-multi-thread",
  "net",
  "io-util",
  "sync",
  "time",
] }

[lints]
workspace = true
