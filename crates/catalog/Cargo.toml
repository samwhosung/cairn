[package]
name = "catalog"
description = "The games cairn carries, looked up by name for the server, the client and the bots"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
game = { path = "../game" }
melee = { path = "../../games/melee" }

[lints]
workspace = true
