[package]
name = "melee"
description = "A game on the rule API: every player fights every other hand to hand, and the dead rise at their spawn"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
game = { path = "../../crates/game" }

[lints]
workspace = true
